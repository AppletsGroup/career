export type Education = {
  institution: string;
  degree: string;
  duration: string;
}

export type Resume = {
  id: number
  title: string;
  description: string;
  location: string;
  skills: string[];
}
